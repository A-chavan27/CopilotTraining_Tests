// create add number function
// add a number to a comment
// return the new number of the comment
// return the new number of the comment
function addNumber(comment) {
  comment.number = comment.number + 1;
  return comment.number;
}